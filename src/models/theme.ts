export interface Theme {
    name: string;
    colors: {
      background: string;
      text: string;
      primary: string;
      secondary: string;
      tertiary: string;
      // Add more color properties as needed
    };
  }